# Shree Sai Traders - Frontend (Template)

This is a generated React + Vite + Tailwind frontend template for *Shree Sai Traders*.

How to use:
1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`
3. Build: `npm run build`

Notes:
- Assets are placeholders in `public/assets` — replace with real images.
- The project is mobile-first and uses Tailwind CSS.
